spring:
  jpa:
    hibernate:
      ddl-auto: create-drop

security:
  jwt:
    secret: test_secret_key_for_order_service_tests_which_is_long_enough
    expiration: 3600000

product-service:
  grpc:
    address: localhost:9090
